/// @description Setup
options = [
	"PLAY", "OPTIONS", "HANGAR", "EXIT"
]

menu_index = 0;
menu_index_max = array_length(options) - 1;
menu_space = 60;
x_pos = room_width / 2;
y_pos = room_height / 2;